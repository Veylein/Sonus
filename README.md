# Sonus

Sonus — an audio operating system for Discord. This repository contains the Python scaffold for Sonus with modular audio, commands, UI, and data layers.

See `README.md` in the root for basic usage. Run `python main.py` to start (set `SONUS_TOKEN` in environment).
